"""The strategy package — the daily agent fills this in for its thesis (config + signal), keeping
the package name stable and updating pyproject `name`/`description`. The reusable rigor lives in
`qf`, not here.
"""
